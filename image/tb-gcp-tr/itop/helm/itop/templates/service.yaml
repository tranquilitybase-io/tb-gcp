apiVersion: v1
kind: Service
metadata:
  name: {{ include "itop.fullname" . }}
  labels:
    app: {{ include "itop.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "itop.name" . }}
