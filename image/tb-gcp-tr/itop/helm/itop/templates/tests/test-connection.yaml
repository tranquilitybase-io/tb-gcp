apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itop.fullname" . }}-test-connection"
  labels:
{{ include "itop.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "itop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

